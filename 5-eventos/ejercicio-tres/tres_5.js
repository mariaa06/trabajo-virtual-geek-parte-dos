const button = document.querySelector('p');

button.addEventListener('mouseover', () => document.querySelector ('p').innerHTML = 'It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged.');

